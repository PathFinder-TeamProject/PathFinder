server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
  data:
    redis:
      database: 0
  kafka:
    consumer:
      group-id: user-service-group

logging:
  level:
    com.pathfinder.user: DEBUG
